[project]
name = "hexlet-flask-example"
version = "0.1.0"
description = "My first flask app"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flake8>=7.1.2",
    "flask>=3.1.0",
    "gunicorn>=23.0.0",
    "psycopg2>=2.9.10",
]

[tool.uv.workspace]
members = ["venv"]
